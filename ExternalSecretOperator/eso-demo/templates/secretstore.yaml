---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: {{ .Values.secretStore.name }}
  namespace: {{ .Release.Namespace }}
spec:
  provider:
    fake:
      data:
        - key: {{ .Values.fakeSecret.key | quote }}
          value: {{ .Values.fakeSecret.apiKey | quote }}
